#include "main.h"
/**
 * _abs - a program that computes absolute values of integers
 * @n: numaber to be computes
 * Return: absolute value of number
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
