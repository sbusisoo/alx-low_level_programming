#include <stdio.h>
/**
 * main - a program that prints from 0 to 10
 * Return: 0 success
 */
int main(void)
{
	int g;

	for (g = 0; g < 10; g++)
	{
		printf("%d", g);
	}
	putchar('\n');
	return (0);
}
