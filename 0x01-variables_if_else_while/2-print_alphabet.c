#include <stdio.h>
/**
 * main - a program that prints the alphabet in low case
 * Return: 0
 */

int main(void)
{
	char a;

	c = 'a';
	while (c <= 'z')
	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
