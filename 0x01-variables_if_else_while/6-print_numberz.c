#include <stdio.h>
/**
 * main - a program that prints numbers 1 to 10
 * Return: 0 success
 */
int main(void)
{
	int l;

	for (l = 48; l < 58; l++)
	{
		putchar(l);
	}
	putchar('\n');
	return (0);
}
